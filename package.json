{
  "name": "@geee-be/react-libraries",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "license": "MIT",
  "author": "Greg Bacchus",
  "type": "module",
  "scripts": {
    "build": "pnpm --stream --parallel -r build",
    "changes": "changeset && git add .changeset",
    "changeset": "changeset",
    "lint": "pnpm --stream -r lint",
    "lint:biome": "pnpm --stream -r lint:biome",
    "lint:biome:fix": "pnpm --stream -r lint:biome:fix",
    "start": "pnpm --stream -r start",
    "start:dev": "pnpm --stream --parallel -r start:dev"
  },
  "devDependencies": {
    "@biomejs/biome": "1.7.1",
    "@changesets/cli": "2.29.5",
    "@swc/cli": "^0.1.65",
    "@swc/core": "^1.12.6",
    "@types/node": "^20.19.1",
    "chokidar": "^3.6.0",
    "esbuild": "^0.17.19",
    "rimraf": "^5.0.10",
    "ts-unused-exports": "^9.0.5",
    "typescript": "^5.8.3"
  }
}
